apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cats-dogs-api-production
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: cats-dogs-mlops
    environment: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    # Auto-sync on Git changes
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: cats-dogs-mlops
  source:
    repoURL: https://github.com/swapniljain782/cats-dogs-mlops.git
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: mlops-cats-dogs
  syncPolicy:
    automated:
      prune: true       # Delete resources removed from Git
      selfHeal: true     # Revert manual changes in cluster
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
  # Health checks
  info:
    - name: Description
      value: "Cats vs Dogs Classification API - Production"
    - name: Team
      value: "MLOps"
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas  # Allow autoscaler to manage replicas
